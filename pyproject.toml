[build-system]
requires = ["setuptools >= 75.3.2"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version"]
name = "snowflake-security"


[project.scripts]
snowflake = "snowflake.cli:main"
